class SoundThing : ZilchComponent
{
    function Initialize(init : CogInitializer)
    {
        this.Owner.SoundEmitter.PlayCue(SoundCue.Find("hey"));
    }

    function OnLogicUpdate(event : UpdateEvent)
    {
    }
}
